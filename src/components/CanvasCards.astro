---
const tools = [
  { name: 'Google Ads', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M3.9 12c0-1.71 1.39-3.1 3.1-3.1h4V7H7a5 5 0 0 0 0 10h4v-1.9H7c-1.71 0-3.1-1.39-3.1-3.1M8 13h8v-2H8zm9-6h-4v1.9h4c1.71 0 3.1 1.39 3.1 3.1s-1.39 3.1-3.1 3.1h-4V17h4a5 5 0 0 0 0-10"/></svg>' },
  { name: 'Meta Ads', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.04c-5.5 0-10 4.49-10 10.02 0 5 3.66 9.15 8.44 9.9v-7H7.9v-2.9h2.54V9.85c0-2.51 1.49-3.89 3.78-3.89 1.09 0 2.23.19 2.23.19v2.47h-1.26c-1.24 0-1.63.77-1.63 1.56v1.88h2.78l-.45 2.9h-2.33v7a10 10 0 0 0 8.44-9.9c0-5.53-4.5-10.02-10-10.02z"/></svg>' },
  { name: 'Analytics', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2M9 17H7v-7h2zm4 0h-2V7h2zm4 0h-2v-4h2z"/></svg>' },
  { name: 'Figma', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M8.5 2A3.5 3.5 0 0 0 5 5.5 3.5 3.5 0 0 0 8.5 9h3V2zM13 2v7h3.5A3.5 3.5 0 0 0 20 5.5 3.5 3.5 0 0 0 16.5 2zm-4.5 8.5A3.5 3.5 0 0 0 5 14a3.5 3.5 0 0 0 3.5 3.5h3v-7zM13 14a3.5 3.5 0 1 0 7 0 3.5 3.5 0 0 0-7 0m-4.5 5A3.5 3.5 0 0 0 12 22.5 3.5 3.5 0 0 0 8.5 19"/></svg>' },
  { name: 'SEMrush', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2m-1 15H8v-2h3zm5-4H8v-2h8zm0-4H8V7h8z"/></svg>' },
  { name: 'Shopify', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M18.572 5.881l-.857-.072s-.855-.855-.998-.998c-.072-.072-.214-.05-.302-.024l-.42.13a4.67 4.67 0 0 0-.317-.774c-.47-.901-1.158-1.377-1.988-1.377-.058 0-.116.002-.173.008-.025-.03-.05-.06-.078-.088C13.972 2.18 13.38 2 12.68 2c-1.373 0-2.74.97-3.854 2.73-.784 1.24-1.38 2.8-1.548 4.01l-2.14.663c-.63.198-.65.218-.732.813-.063.45-1.706 13.15-1.706 13.15L15.19 25.5l7.11-1.538s-3.65-17.826-3.685-18.005c-.036-.178-.145-.068-.043-.076zm-4.01-1.032l-.675.21a7.4 7.4 0 0 0-.503-1.563c.622.117 1.035.79 1.178 1.353m-1.758.544l-1.45.449c.28-1.075.81-2.146 1.456-2.85.242-.263.578-.554.96-.72-.252.555-.41 1.343-.466 2.121-.016.199-.027.557-.027.557l.527-.163zM12.65 2.7c.312 0 .574.065.81.196-.362.193-.712.494-1.044.86-.88.97-1.554 2.475-1.823 3.926l-1.185.367C9.8 6.47 11.08 2.7 12.65 2.7"/></svg>' },
  { name: 'WordPress', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.486 2 2 6.486 2 12s4.486 10 10 10 10-4.486 10-10S17.514 2 12 2m-1.5 15.08L6.9 7.5c.4-.02.77-.05.77-.05.36-.04.32-.57-.04-.55 0 0-1.09.09-1.79.09l-.3-.01C7.21 4.7 9.45 3.5 12 3.5c1.89 0 3.61.72 4.9 1.9h-.12c-.7 0-1.19.6-1.19 1.26 0 .58.34 1.08.7 1.66.27.47.59 1.07.59 1.93 0 .6-.23 1.29-.54 2.26l-.71 2.37-2.56-7.61c.4-.02.77-.05.77-.05.36-.04.32-.57-.04-.55 0 0-1.09.09-1.79.09-.66 0-1.77-.09-1.77-.09-.36-.02-.4.53-.04.55 0 0 .34.03.72.05zm1.5 .42l2.12-6.17c.4-1 .53-1.8.53-2.51 0-.26-.02-.5-.05-.72A8.46 8.46 0 0 1 20.5 12c0 3.25-1.83 6.07-4.52 7.5l2.77-8a7.77 7.77 0 0 0 .56-2.75c0-.28-.02-.55-.06-.81A8.46 8.46 0 0 1 20.5 12a8.5 8.5 0 0 1-4.25 7.37zm-2.07.07L7.1 8.66 12 17.55z"/></svg>' },
  { name: 'Hotjar', svg: '<svg viewBox="0 0 24 24" fill="currentColor"><path d="M13.54 2S9 6.39 9 11.31c0 2.48 1.66 3.69 3.53 3.69 2.29 0 3.34-1.81 3.34-3.33 0-1.13-.68-2.2-2.15-2.2-.89 0-1.63.54-1.87 1.31-.08.25-.06.5-.06.76 0 .6.35 1.12.88 1.12.47 0 .73-.35.73-.78 0 0 .03-.14-.06-.14s-.23.12-.3.12-.22-.12-.22-.35c0-.36.22-.68.57-.68.45 0 .72.4.72.87 0 .82-.55 1.57-1.35 1.57-1.01 0-1.53-.83-1.53-1.78 0-1.71 1.38-3.28 3.83-3.28 2.12 0 3.59 1.38 3.59 3.53 0 2.56-1.68 5.31-5.87 5.31-3.56 0-5.56-2.67-5.56-5.43A12.03 12.03 0 0 1 13.54 2"/></svg>' },
];
---

<section class="canvas-cards" id="stack">
  <div class="container canvas-cards__inner">
    <div class="canvas-cards__text" data-animate>
      <span class="canvas-cards__label">Technologijos</span>
      <h2 class="canvas-cards__title" data-i18n="canvas.title">KODAS,<br/>KURIS<br/>DIRBA.</h2>
      <p class="canvas-cards__desc" data-i18n="canvas.desc">
        Kiekvienas mūsų projektas gimsta iš švaraus kodo, greito dizaino ir duomenimis pagrįstų sprendimų. Nuo svetainės iki reklamos — viskas veikia kaip vieninga sistema.
      </p>
      <div class="canvas-cards__tools" data-animate data-animate-delay="1">
        <span class="canvas-cards__tools-label" data-i18n="stack.label">Dirbame su</span>
        <div class="canvas-cards__tools-row">
          {tools.map((tool) => (
            <div class="canvas-cards__tool">
              <span class="canvas-cards__tool-icon" set:html={tool.svg}></span>
              <span class="canvas-cards__tool-name">{tool.name}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
    <div class="canvas-cards__canvas-wrap" data-animate data-animate-delay="2">
      <canvas id="code-canvas"></canvas>
    </div>
  </div>
</section>

<script>
  const canvas = document.getElementById('code-canvas') as HTMLCanvasElement;
  if (canvas) {
    const ctx = canvas.getContext('2d')!;
    const wrap = canvas.parentElement!;
    const dpr = window.devicePixelRatio || 1;
    let w: number, h: number;

    function resizeCanvas() {
      const rect = wrap.getBoundingClientRect();
      w = rect.width;
      h = rect.height;
      canvas.width = w * dpr;
      canvas.height = h * dpr;
      canvas.style.width = rect.width + 'px';
      canvas.style.height = rect.height + 'px';
      ctx.setTransform(1, 0, 0, 1, 0, 0);
      ctx.scale(dpr, dpr);
    }

    const lines = [
      { text: 'const launch = async () => {', color: '#c8f73a', indent: 0 },
      { text: '  const strategy = analyze(market);', color: '#8b8b8b', indent: 0 },
      { text: '  const design = await create({', color: '#8b8b8b', indent: 0 },
      { text: '    brand: client.vision,', color: '#6B8FF0', indent: 0 },
      { text: '    ux: "premium",', color: '#6B8FF0', indent: 0 },
      { text: '    mobile: true,', color: '#6B8FF0', indent: 0 },
      { text: '  });', color: '#8b8b8b', indent: 0 },
      { text: '  const site = build(design);', color: '#8b8b8b', indent: 0 },
      { text: '  const ads = startCampaigns({', color: '#8b8b8b', indent: 0 },
      { text: '    google: trackROI(),', color: '#FF8C42', indent: 0 },
      { text: '    meta: targetAudience(),', color: '#FF8C42', indent: 0 },
      { text: '    budget: optimize()', color: '#FF8C42', indent: 0 },
      { text: '  });', color: '#8b8b8b', indent: 0 },
      { text: '  return { site, ads, growth };', color: '#c8f73a', indent: 0 },
      { text: '};', color: '#c8f73a', indent: 0 },
      { text: '', color: '#8b8b8b', indent: 0 },
      { text: 'launch(); // 🚀 rezultatai', color: '#00D9A6', indent: 0 },
    ];

    let t = 0;
    const charSpeed = 0.35;

    // Floating particles
    const particles = Array.from({ length: 30 }, () => ({
      x: Math.random() * 1,
      y: Math.random() * 1,
      vx: (Math.random() - 0.5) * 0.0005,
      vy: (Math.random() - 0.5) * 0.0005,
      size: 0.5 + Math.random() * 1.5,
      alpha: 0.1 + Math.random() * 0.2,
    }));

    function draw() {
      t += charSpeed;
      ctx.clearRect(0, 0, w, h);

      // Floating background particles
      particles.forEach((p) => {
        p.x += p.vx;
        p.y += p.vy;
        if (p.x < 0 || p.x > 1) p.vx *= -1;
        if (p.y < 0 || p.y > 1) p.vy *= -1;
        ctx.beginPath();
        ctx.arc(p.x * w, p.y * h, p.size, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(200,247,58,${p.alpha})`;
        ctx.fill();
      });

      // Code lines
      const fontSize = Math.max(12, Math.min(14, w * 0.035));
      const lineHeight = fontSize * 1.75;
      const startX = 28;
      const startY = 36;
      let charCount = 0;

      // Line numbers gutter
      lines.forEach((line, i) => {
        const y = startY + i * lineHeight;
        if (y > h - 20) return;
        ctx.font = `${fontSize}px ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace`;
        ctx.textBaseline = 'top';

        // Line number
        ctx.fillStyle = 'rgba(255,255,255,0.1)';
        ctx.textAlign = 'right';
        ctx.fillText(String(i + 1), 18, y);
        ctx.textAlign = 'left';

        // Code text — typed effect
        const visibleChars = Math.max(0, Math.floor(t) - charCount);
        const displayText = line.text.substring(0, Math.min(visibleChars, line.text.length));
        charCount += line.text.length;

        if (displayText.length > 0) {
          ctx.fillStyle = line.color;
          ctx.fillText(displayText, startX, y);

          // Cursor blink at end of current line
          if (visibleChars <= line.text.length && visibleChars > 0) {
            const cursorX = startX + ctx.measureText(displayText).width;
            if (Math.sin(Date.now() * 0.006) > 0) {
              ctx.fillStyle = '#c8f73a';
              ctx.fillRect(cursorX + 2, y, 2, fontSize);
            }
          }
        }
      });

      // Loop the typing after all lines
      const totalChars = lines.reduce((s, l) => s + l.text.length, 0);
      if (Math.floor(t) > totalChars + 40) t = 0;

      requestAnimationFrame(draw);
    }

    // Wait for full page load so layout is complete before measuring
    window.addEventListener('load', () => {
      resizeCanvas();
      requestAnimationFrame(draw);
    });

    window.addEventListener('resize', resizeCanvas);
  }
</script>

<style>
  .canvas-cards {
    padding: 80px 0;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .canvas-cards__inner {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 48px;
    align-items: start;
  }

  .canvas-cards__label {
    font-size: 10px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--accent);
    display: block;
    margin-bottom: 16px;
  }

  .canvas-cards__title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(48px, 5vw, 72px);
    line-height: 0.9;
    font-weight: 400;
    margin-bottom: 20px;
  }

  .canvas-cards__desc {
    font-size: 14px;
    color: var(--text-muted);
    line-height: 1.7;
    max-width: 340px;
  }

  /* Stack tools integrated */
  .canvas-cards__tools {
    margin-top: 32px;
    padding-top: 24px;
    border-top: 1px solid var(--border);
  }

  .canvas-cards__tools-label {
    display: block;
    font-size: 9px;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--text-muted);
    margin-bottom: 16px;
  }

  .canvas-cards__tools-row {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .canvas-cards__tool {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    transition: border-color 0.3s, background 0.3s;
    cursor: default;
  }

  .canvas-cards__tool:hover {
    background: rgba(200, 247, 58, 0.04);
    border-color: rgba(200, 247, 58, 0.2);
  }

  .canvas-cards__tool-icon {
    width: 16px;
    height: 16px;
    color: var(--text-muted);
    transition: color 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .canvas-cards__tool-icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  .canvas-cards__tool:hover .canvas-cards__tool-icon {
    color: var(--accent);
  }

  .canvas-cards__tool-name {
    font-size: 9px;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: var(--text-muted);
    transition: color 0.2s;
    white-space: nowrap;
  }

  .canvas-cards__tool:hover .canvas-cards__tool-name {
    color: var(--accent);
  }

  .canvas-cards__canvas-wrap {
    background: #111;
    border-radius: 16px;
    overflow: hidden;
    aspect-ratio: 4 / 3;
    position: relative;
    border: 1px solid var(--border);
  }

  .canvas-cards__canvas-wrap canvas {
    width: 100%;
    height: 100%;
    display: block;
  }

  @media (max-width: 768px) {
    .canvas-cards__inner {
      grid-template-columns: 1fr;
    }

    .canvas-cards__canvas-wrap {
      aspect-ratio: 1 / 1;
    }
  }
</style>
